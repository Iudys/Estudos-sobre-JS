let numero = 1;
{
    let numero = 2;
    console.log("dentro =", numero);
}
console.log("fora =", numero);

// palavras reservadas com a palavra var tem escopo global e tem escopo de função e palavras definidas com a palavra let tem escopo global, de função e de bloco
